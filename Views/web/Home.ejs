<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #e8f2ef;
            font-family: Arial, sans-serif;
        }

        .navbar-brand {
            font-weight: bold;
            color: #7460ee !important;
        }

        .blog-title {
            margin: 40px 0 20px;
            font-weight: bold;
        }

        .card img {
            height: 200px;
            object-fit: cover;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm">
        <div class="container">

            <a class="navbar-brand fw-bold" href="/web">Blog</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-lg-center gap-3">
                    <li class="nav-item"><a class="nav-link" href="/web">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/web">Blog</a></li>
                    <li class="nav-item"><a class="nav-link" href="/web/contact">Contact</a></li>

                    <li class="nav-item"><a class="nav-link" href="/web/login">Login</a></li>
                    <li class="nav-item">
                        <a href="/web/register" class="btn text-white" style="background-color: #7460ee;">Register</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Content -->
        
    <div class="blog">
        <h2 class="blog-title text-center py-5 mb-2" style="background-color: #7460ee; color: #fff;"> Blogs</h2>
        <div class="container mt-5">
            <div class="row">
            <% if (blogs.length === 0) { %>
                <p>No blogs found. Start writing one!</p>
            <% } %>

            <% blogs.forEach(blog => { %>
                <div class="col-md-3 mb-5">
                    <div class="card ">
                        <img src="<%= blog.image %>" class="card-img-top" height="200">
                        <div class="card-body">
                            <h5 class="card-title"><%= blog.title %></h5>
                            <h6 class="card-title text-secondary pt-2">By:-<%= blog.author %></h6>
                            <p class="text-muted pb-0">Category: <%= blog.category %></p>
                            <p class="text-muted">Date: <%= new Date(blog.createdAt).toLocaleDateString() %></p>
                           <a href="/web/singleblog/<%= blog._id %>" class="btn  btn-sm" style="background-color: #7460ee; color: #fff;">
                                <i class="fas fa-eye"></i> Read More
                            </a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>